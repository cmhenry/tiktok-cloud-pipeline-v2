---
# Production Inventory for Audio Processing Pipeline
# Replace placeholder IPs (10.0.0.X) with actual VM addresses

all:
  children:
    orchestrator:
      hosts:
        orch-01:
          ansible_host: 172.23.207.33 # CHANGE: Orchestrator VM IP

    gpu_workers:
      hosts:
        gpu-01:
          ansible_host: 172.23.208.245  # CHANGE: GPU Worker 1 IP
        # gpu-02:
          # ansible_host: 10.0.0.11  # CHANGE: GPU Worker 2 IP
        # Add more GPU workers as needed:
        # gpu-03:
        #   ansible_host: 10.0.0.12

    transfer:
      hosts:
        transfer-01:
          ansible_host: 172.23.207.33  # CHANGE: Transfer Worker IP
          # Note: This may be the same as orchestrator

  vars:
    ansible_user: ubuntu
    ansible_python_interpreter: /usr/bin/python3
